<h2>Customers list</h2>
<ul>
    {{#each customersList}} {{! reference to customerList from 'customer-router' file }}
        <li>
            <label>
                {{this.fullName}}
                <select>
                    <option value="">No products in use</option>
                    {{#each ../productsList}} {{! "../" allows to go out from the context of first each and define productsList }}
                        <option value="{{this.productName}}">{{this.productName}}</option>
                    {{/each}}
                </select>
            </label>
            <button>Save</button>
        </li>
    {{/each}}
</ul>
<h2>Add new customer</h2>
<form action="/customers" method="POST">
    <label>
        Full Name:
        <input name="fullName" type="text">
    </label>
    <button>Save</button>
</form>